version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./data/countries.db
    volumes:
      - ./cache:/app/cache
      - ./data:/app/data
